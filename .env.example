# ============================
# Variables de base de datos
# ============================
# Nombre de la base de datos que usará la aplicación en MariaDB.
MARIADB_DATABASE=healthyhabits_dev
# Usuario con permisos para acceder a la base de datos definida arriba.
MARIADB_USER=healthyhabits_user
# Contraseña del usuario anterior (modifícala por una segura en tu entorno real).
MARIADB_PASSWORD=changeMeUserPwd123!
# Contraseña del usuario root de MariaDB para despliegues locales con Docker.
MARIADB_ROOT_PASSWORD=changeMeRootPwd123!

# =====================================
# Parámetros de JPA e Hibernate (opcional)
# =====================================
# Modo de gestión del esquema por Hibernate (update, validate, create, etc.).
SPRING_JPA_HIBERNATE_DDL_AUTO=update
# Indica si se deben mostrar las sentencias SQL en el log.
SPRING_JPA_SHOW_SQL=false

# ===============================
# Configuración de autenticación
# ===============================
# Secreto en Base64 usado para firmar tokens JWT (reemplázalo por uno aleatorio y largo).
JWT_SECRET=dGhpc19pc19hX3NhbXBsZV9qd3Rfc2VjcmV0X2tleQ==
# Tiempo de expiración de los tokens JWT en milisegundos (por ejemplo, 24h = 86400000).
JWT_EXPIRATION_MS=86400000
# Valor adicional mezclado con las contraseñas antes de cifrarlas.
PASSWORD_PEPPER=changeMePepperValue

# ===================================
# Credenciales del usuario administrador
# ===================================
# Contraseña inicial del administrador que crea el inicializador al arrancar la app.
APP_ADMIN_PASSWORD=ChangeMeAdmin123!